# Copied to .env by install.sh. Change the DB password before exposing this box.
SONAR_JDBC_PASSWORD=sonar
SONAR_PORT=9000

# Optional JVM overrides (defaults in docker-compose.yml fit a t3.medium).
# SONAR_WEB_JAVAOPTS=-Xmx512m -Xms128m
# SONAR_CE_JAVAOPTS=-Xmx512m -Xms128m
# SONAR_SEARCH_JAVAOPTS=-Xmx512m -Xms512m
